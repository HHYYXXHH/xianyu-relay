# 闲鱼消息转发与接收端处理方案文档

> 说明：本文档用于汇总当前已确认的方案、字段定义、接口定义、优先级与后续落地步骤。后续每次需求变更、字段调整、流程调整或实现完成后，都应更新本文件。

---

## 1. 目标

实现一套”稳定同步 + 本地 OCR + 失败提醒”的闲鱼消息转发系统。

### 目标能力
1. 发射端 Android APK 稳定采集闲鱼消息、图片、交易/转账相关信息。
2. 发射端纯本地处理（通知监听 + OCR），不依赖云端 AI。
3. 中转服务器（Python）接收、存储、去重、推送事件。
4. 接收端 Android APK 展示消息、图片、失败提醒，并支持人工处理。
5. OCR 失败时保留原图并上报给服务器，触发接收端提醒。
6. 普通图片上传失败时也要给接收端报错信息。

### 实现方式

| 端 | 形态 | 技术栈 |
| --- | --- | --- |
| 发射端 | Android APK | NotificationListenerService + Tesseract/ML Kit + OkHttp |
| 中转服务器 | Python 服务 | HTTP(8000) + WebSocket(8001) + SQLite |
| 接收端 | Android APK | OkHttp WebSocket + RecyclerView + Glide + NotificationManager |

> 当前仓库中的 `source_app/` 和 `receiver_app/` 为 **Python 参考实现**，包含完整业务逻辑，可直接运行于 PC 进行验证和调试。Android APK 版本对应的逻辑映射详见第 17 章。

---

## 2. 当前架构

### 2.1 三端结构

- **发射端**（Android APK）：通过 Android 系统通知监听服务，直接采集手机上的闲鱼消息。负责消息分类、本地 OCR、事件上传、失败兜底。
- **中转服务器**（Python）：部署于公网服务器，接收事件、图片存储、去重、推送至接收端、状态维护。
- **接收端**（Android APK）：通过 WebSocket 实时接收服务器推送，展示消息和图片，需关注事件弹通知提醒，支持标记已处理。

### 2.2 流程总览

1. 发射端 APK 监听到闲鱼通知。
2. 发射端识别消息类型（文字、图片、卡片、混合）。
3. 普通图片直接转发上传。
4. 卡片类图片本地 OCR（Android Tesseract / ML Kit）。
5. OCR 成功时转成标准文字信息。
6. OCR 失败时保留原图并上报，触发接收端提醒。
7. 普通图片上传失败时生成失败事件并上报，触发接收端报错提醒。
8. 中转服务器保存并推送至接收端。
9. 接收端 APK 展示消息、图片、失败提醒，并允许人工处理。

---

## 3. 当前处理原则

### 3.1 发射端原则
- 发射端只做稳定同步和本地处理。
- 不在发射端做 AI 回复。
- 不在发射端做自动回复。
- 只处理本地可落地的 OCR 与规则解析。

### 3.2 OCR 原则
- 普通图片不 OCR。
- 卡片类图片做本地 OCR。
- OCR 成功才转成标准文字信息。
- OCR 失败保留原图并上报。
- OCR 失败不阻塞同步。

### 3.3 稳定性原则
- 所有上传失败都要有失败事件。
- 所有失败事件都要传递到接收端。
- 所有图片原图必须保留引用。
- 需要去重、防重复推送。

---

## 4. 当前消息类型定义

### 4.1 普通文字
- 纯文本消息。

### 4.2 普通图片
- 仅图片内容。
- 直接转发。

### 4.3 卡片类图片
- 交易状态类图片。
- 转账类图片。
- 需要 OCR + 简单规则解析。

### 4.4 混合消息
- 文字 + 图片。
- 文字 + 卡片。
- 图片 + 卡片。

---

## 5. 当前图片处理逻辑

### 5.1 普通图片
- 直接转发。
- 不 OCR。
- 如果上传失败，生成上传失败事件，推送接收端。

### 5.2 卡片类图片
- 本地 OCR。
- 解析简单字段：状态、金额、时间、订单号、退款号。
- 生成标准文字摘要。
- OCR 成功时发给接收端。

### 5.3 OCR 失败
- 保留原图。
- 生成失败事件。
- 设置 `ocr_status = failed`。
- 设置 `need_receiver_attention = true`。
- 设置 `notify_receiver = true`。
- 上传到服务器并推送接收端。

---

## 6. 当前字段定义

### 6.1 普通图片事件

```json
{
  "event_id": "evt_001",
  "event_type": "image",
  "content_type": "image",
  "source": "chat_page",
  "timestamp": "2026-05-26 10:20:00",
  "thread_key": "thread_buyer_12345",
  "message_key": "msg_001",
  "image_refs": ["img_001.jpg"],
  "summary": "用户发送了图片",
  "ocr_status": "not_needed",
  "need_receiver_attention": false,
  "notify_receiver": false,
  "upload_status": "pending",
  "checksum": "md5"
}
```

### 6.2 普通图片上传失败事件

```json
{
  "event_id": "evt_002",
  "event_type": "image",
  "content_type": "image",
  "source": "chat_page",
  "timestamp": "2026-05-26 10:21:00",
  "thread_key": "thread_buyer_12345",
  "message_key": "msg_002",
  "image_refs": ["img_002.jpg"],
  "summary": "普通图片上传失败",
  "ocr_status": "not_needed",
  "upload_status": "failed",
  "error_code": "upload_timeout",
  "error_message": "图片上传超时，请检查网络或稍后重试",
  "need_receiver_attention": true,
  "notify_receiver": true,
  "checksum": "md5"
}
```

### 6.3 OCR 失败事件

```json
{
  "event_id": "evt_003",
  "event_type": "image",
  "content_type": "image",
  "source": "chat_page",
  "timestamp": "2026-05-26 10:22:00",
  "thread_key": "thread_buyer_12345",
  "message_key": "msg_003",
  "image_refs": ["img_003.jpg"],
  "summary": "图片识别失败，已保留原图",
  "ocr_status": "failed",
  "ocr_error": "no_text_detected",
  "need_receiver_attention": true,
  "notify_receiver": true,
  "checksum": "md5"
}
```

### 6.4 OCR 成功事件（可选）

```json
{
  "event_id": "evt_004",
  "event_type": "trade_card",
  "content_type": "card",
  "source": "chat_page",
  "timestamp": "2026-05-26 10:23:00",
  "thread_key": "thread_buyer_12345",
  "message_key": "msg_004",
  "image_refs": ["img_004.jpg"],
  "image_ocr_text": "订单已付款\n订单号：order_12345\n金额：199元",
  "content_text": "订单已付款",
  "summary": "订单已付款，订单号：order_12345",
  "ocr_status": "success",
  "need_receiver_attention": false,
  "notify_receiver": false,
  "checksum": "md5"
}
```

---

## 7. 当前接口定义

### 7.1 发射端 → 中转服务器

#### 接口
- `POST /events`

#### 请求示例
```json
{
  "event_id": "evt_001",
  "event_type": "image",
  "content_type": "image",
  "source": "chat_page",
  "timestamp": "2026-05-26 10:20:00",
  "thread_key": "thread_buyer_12345",
  "message_key": "msg_001",
  "image_refs": ["img_001.jpg"],
  "summary": "用户发送了图片",
  "ocr_status": "not_needed",
  "need_receiver_attention": false,
  "notify_receiver": false,
  "upload_status": "pending",
  "checksum": "md5"
}
```

#### 响应示例
```json
{
  "status": "accepted",
  "event_id": "evt_001"
}
```

### 7.2 中转服务器 → 接收端推送

#### 推送触发条件
- `notify_receiver = true`

#### 推送内容
- 保持与事件结构一致，至少包含：
  - `event_id`
  - `event_type`
  - `content_type`
  - `thread_key`
  - `message_key`
  - `image_refs`
  - `summary`
  - `ocr_status`
  - `upload_status`
  - `error_code`
  - `error_message`
  - `need_receiver_attention`
  - `notify_receiver`

### 7.3 接收端 → 中转服务器处理回传

#### 接口
- `POST /attention-status`

#### 请求示例
```json
{
  "event_id": "evt_002",
  "attention_status": "handled",
  "handled_at": "2026-05-26 10:25:00"
}
```

#### 响应示例
```json
{
  "status": "ok"
}
```

## 7.4 第一阶段接口字段完整定义

### 7.4.1 发射端 → 中转服务器：`POST /events`

#### 通用字段定义

| 字段名 | 类型 | 必填 | 说明 | 取值范围 / 示例 |
| --- | --- | --- | --- | --- |
| `event_id` | string | 是 | 事件唯一 ID | `evt_001` |
| `event_type` | string | 是 | 事件类型 | `image` |
| `content_type` | string | 是 | 内容类型 | `image` / `card` |
| `source` | string | 是 | 消息来源 | `chat_page` |
| `timestamp` | string | 是 | 事件生成时间 | `2026-05-26 10:20:00` |
| `thread_key` | string | 是 | 会话唯一键 | `thread_buyer_12345` |
| `message_key` | string | 是 | 消息唯一键 | `msg_001` |
| `image_refs` | array<string> | 是 | 图片引用列表 | `["img_001.jpg"]` |
| `summary` | string | 是 | 简短摘要 | `用户发送了图片` |
| `ocr_status` | string | 是 | OCR 状态 | `not_needed` / `success` / `failed` |
| `upload_status` | string | 是 | 上传状态 | `pending` / `success` / `failed` |
| `need_receiver_attention` | boolean | 是 | 是否需要接收端关注 | `true` / `false` |
| `notify_receiver` | boolean | 是 | 是否推送接收端 | `true` / `false` |
| `checksum` | string | 是 | 内容校验值 | `md5` |
| `content_text` | string | 否 | 纯文本内容 | `订单已付款` |
| `image_ocr_text` | string | 否 | OCR 结果文本 | `订单已付款\n订单号：order_12345` |
| `ocr_error` | string | 否 | OCR 失败原因 | `no_text_detected` |
| `error_code` | string | 否 | 错误码 | `upload_timeout` |
| `error_message` | string | 否 | 错误信息 | `图片上传超时，请检查网络或稍后重试` |
| `retry_count` | number | 否 | 重试次数 | `0` / `1` |
| `handled_at` | string | 否 | 处理完成时间 | `2026-05-26 10:25:00` |

#### 字段使用规则

- 普通图片成功事件：
  - `ocr_status = not_needed`
  - `upload_status = success`
  - `need_receiver_attention = false`
  - `notify_receiver = false`

- 普通图片上传失败事件：
  - `ocr_status = not_needed`
  - `upload_status = failed`
  - `need_receiver_attention = true`
  - `notify_receiver = true`
  - 必填：`error_code`、`error_message`

- OCR 失败事件：
  - `ocr_status = failed`
  - `upload_status = success`
  - `need_receiver_attention = true`
  - `notify_receiver = true`
  - 必填：`ocr_error`

#### 请求示例

```json
{
  "event_id": "evt_002",
  "event_type": "image",
  "content_type": "image",
  "source": "chat_page",
  "timestamp": "2026-05-26 10:21:00",
  "thread_key": "thread_buyer_12345",
  "message_key": "msg_002",
  "image_refs": ["img_002.jpg"],
  "summary": "普通图片上传失败",
  "ocr_status": "not_needed",
  "upload_status": "failed",
  "need_receiver_attention": true,
  "notify_receiver": true,
  "checksum": "md5",
  "error_code": "upload_timeout",
  "error_message": "图片上传超时，请检查网络或稍后重试"
}
```

#### 响应示例

```json
{
  "status": "accepted",
  "event_id": "evt_002"
}
```

### 7.4.2 中转服务器 → 接收端推送

#### 推送字段定义

推送内容与 `POST /events` 请求体保持一致，必须至少包含以下字段：

| 字段名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `event_id` | string | 是 | 事件唯一 ID |
| `event_type` | string | 是 | 事件类型 |
| `content_type` | string | 是 | 内容类型 |
| `thread_key` | string | 是 | 会话唯一键 |
| `message_key` | string | 是 | 消息唯一键 |
| `image_refs` | array<string> | 是 | 图片引用列表 |
| `summary` | string | 是 | 简短摘要 |
| `ocr_status` | string | 是 | OCR 状态 |
| `upload_status` | string | 是 | 上传状态 |
| `error_code` | string | 否 | 错误码 |
| `error_message` | string | 否 | 错误信息 |
| `need_receiver_attention` | boolean | 是 | 是否需要接收端关注 |
| `notify_receiver` | boolean | 是 | 是否推送接收端 |

#### 推送示例

```json
{
  "event_id": "evt_003",
  "event_type": "image",
  "content_type": "image",
  "thread_key": "thread_buyer_12345",
  "message_key": "msg_003",
  "image_refs": ["img_003.jpg"],
  "summary": "图片识别失败，已保留原图",
  "ocr_status": "failed",
  "upload_status": "success",
  "error_code": null,
  "error_message": null,
  "need_receiver_attention": true,
  "notify_receiver": true
}
```

### 7.4.3 接收端 → 中转服务器：`POST /attention-status`

#### 字段定义

| 字段名 | 类型 | 必填 | 说明 | 取值范围 |
| --- | --- | --- | --- | --- |
| `event_id` | string | 是 | 事件唯一 ID | `evt_002` |
| `attention_status` | string | 是 | 处理状态 | `handled` |
| `handled_at` | string | 是 | 处理时间 | `2026-05-26 10:25:00` |

#### 请求示例

```json
{
  "event_id": "evt_002",
  "attention_status": "handled",
  "handled_at": "2026-05-26 10:25:00"
}
```

#### 响应示例

```json
{
  "status": "ok"
}
```

---

## 8. 当前优先级

### 优先级 1（✅ Python 参考已全部完成）
1. 普通图片直接转发 ✅
2. 普通图片上传失败时，错误信息必须传给接收端 ✅
3. OCR 失败事件上传 ✅
4. 接收端识别 OCR 失败事件并提醒展示 ✅

### 优先级 2（✅ Python 参考已全部完成）
1. 卡片类图片 OCR ✅
2. OCR 结果规则解析 ✅
3. 标准文本摘要生成 ✅

### 优先级 3（待 Android APK 阶段）
1. 更复杂卡片样式支持
2. 更稳健的图片分类
3. OCR 结果增强

---

## 9. 当前发射端函数级职责（Python 参考实现）

以下为 Python 参考实现中的函数职责。Android APK 版本需将对应的逻辑映射为 Android 原生实现。

### 9.1 入口函数
- `handleIncomingMessage` → Android 端为 `onNotificationPosted()` 回调

### 9.2 图片处理函数
- `extractImages` → Android BitmapFactory / ContentResolver 加载通知中的图片 URI
- `classifyImage` → 基于 Bitmap 宽高比判断（同 Python 逻辑）
- `handleNormalImage` → Android OkHttp 直接上传
- `handleCardImage` → 调用 Tesseract/ML Kit OCR
- `runLocalOcr` → `tess-two` 库或 Google ML Kit Text Recognition
- `parseOcrResult` → 同 Python 正则逻辑（Java/Kotlin 中可用 `Pattern`）
- `buildCardEvent` → 同 JSON 结构
- `buildOcrFailedEvent` → 同 JSON 结构

### 9.3 上传与失败处理
- `uploadEvent` → OkHttp POST JSON
- `handleUploadFailure` → 生成失败事件的逻辑
- `buildUploadFailureEvent` → 同 JSON 结构
- `enqueueRetry` → Android WorkManager 调度后台重试

### 9.4 Android 关键实现差异

| Python 参考 | Android 实现 |
| --- | --- |
| `message_listener.py` 通过 ADB dumpsys 获取通知 | `NotificationListenerService.onNotificationPosted()` 系统回调 |
| PIL 图片分析 | `Bitmap.getWidth()/getHeight()` |
| Tesseract CLI 调用 | `tess-two` 库或 ML Kit |
| urllib HTTP | OkHttp / Retrofit |
| 本地文件重试队列 | WorkManager + Room 数据库 |

---

## 10. 当前中转服务器职责

中转服务器采用 Python 实现（当前仓库可直接部署），是唯一需要传统服务器部署的部分。

### 10.1 接收与存储
- 接收事件（`POST /events`）
- 接收图片（`POST /images`）
- 保存事件主表（SQLite）
- 保存图片引用
- 保存 OCR 状态与失败信息

### 10.2 推送与提醒
- 根据 `notify_receiver` 决定是否推送
- WebSocket 实时推送到接收端 APK（`ws://IP:8001`）
- HTTP 轮询回退（`GET /events/next`）
- 去重（event_id + checksum 双重校验）
- 维护处理状态（`POST /attention-status`）

### 10.3 部署形态
- 部署于公网 VPS 或任意有公网 IP 的机器
- 监听 8000 (HTTP) + 8001 (WebSocket) 端口
- 对客户端（Android APK）无平台要求，纯 HTTP/WebSocket 标准协议

---

## 11. 当前接收端职责

### 11.1 展示（Android APK）
- 普通图片展示（Glide 加载 `GET /images/<filename>`）
- OCR 成功标准文本展示（RecyclerView 列表项）
- OCR 失败原图展示 + 失败原因标注
- 普通图片上传失败报错展示（错误码 + 错误信息）

### 11.2 提醒（Android APK）
- `need_receiver_attention = true` 的事件 → `NotificationManager` 系统通知栏弹窗
- OCR 失败提醒
- 普通图片上传失败提醒
- 标记已处理（点击按钮 → `POST /attention-status`）

### 11.3 Android 关键实现差异

| Python 参考 | Android 实现 |
| --- | --- |
| `event_receiver.py` websockets 库 | OkHttp WebSocket 连接 |
| `ui_renderer.py` 终端输出 | RecyclerView + Adapter |
| `reminder_manager.py` 终端打印 | NotificationManager 系统通知 |
| `image_loader.py` PIL + urllib | Glide / Coil（自带缓存） |
| `status_reporter.py` HTTP POST | OkHttp POST |
| `gui.py` Tkinter 窗口 | Android Activity + Fragment |

---

## 12. 当前已确认的失败处理规则

### 12.1 普通图片上传失败
- 生成失败事件
- 保留图片引用
- 传给接收端
- 接收端展示报错

### 12.2 OCR 失败
- 保留原图
- 生成失败事件
- 传给接收端
- 接收端展示原图并提醒

### 12.3 OCR 成功
- 生成标准文本信息
- 传给接收端
- 可保留原图（可选）

---

## 13. 当前本地处理定论

### 13.1 发射端（Android APK）本地可执行
- 通知监听（`NotificationListenerService`，系统级回调，无需网络）
- 图片分类（基于 Bitmap 宽高比，毫秒级）
- 普通图片转发上传（OkHttp multipart）
- 卡片类图片 OCR（Android Tesseract `tess-two` 或 Google ML Kit，纯本地）
- OCR 失败事件生成
- 普通图片上传失败事件生成
- 失败重试（WorkManager 后台调度）

### 13.2 接收端（Android APK）本地可执行
- WebSocket 实时接收（OkHttp WebSocket）
- 事件列表展示（RecyclerView）
- 图片加载（Glide，自动缓存）
- 系统通知提醒（NotificationManager）
- 处理状态回传（OkHttp POST）

### 13.3 中转服务器（Python）
- 无需安装在手机端
- 部署于公网服务器，三端通信枢纽
- 当前仓库代码可直接部署

### 13.4 目前不做
- 云端 OCR（已在手机本地完成）
- AI 自动回复
- 自动发送消息
- 复杂卡片识别增强

---

## 14. 后续更新规则

后续每次发生以下任一项变化时，必须更新本文档：

1. 字段变更
2. 接口变更
3. 新增/删除流程
4. 优先级调整
5. 识别规则调整
6. 失败处理规则调整
7. 端到端交互变化

### 更新要求
- 在本文件中补充变更说明
- 在文档中标记更新时间
- 若有新流程或新字段，必须补齐示例
- 若有已完成项，需同步更新完成状态

---

## 15. 当前建议的下一步工作

1. ~~按照本文档完成优先级 1 的实现。~~ ✅ 已完成（详见第 17 章）
2. ~~完成发射端普通图片失败与 OCR 失败事件上报。~~ ✅ 已完成
3. ~~完成中转服务器推送失败事件。~~ ✅ 已完成
4. ~~完成接收端提醒展示。~~ ✅ 已完成
5. ~~在实现完成后，回过头更新本文档并补充”已实现内容”。~~ ✅ 已完成（详见第 17 章）

## 15.1 优先级 1 实现任务拆解

### 15.1.1 发射端任务

1. 接入消息监听与消息抽取
   - 识别消息来源与消息唯一键。
   - 读取文本、图片、附件引用。
   - 生成基础事件元信息。

2. 图片分类
   - 区分普通图片与卡片类图片。
   - 对无法判定的图片采用保守策略，优先进入 OCR 失败兜底流程。

3. 普通图片处理
   - 直接生成普通图片事件。
   - 上传事件与图片引用。
   - 失败时生成 `upload_status = failed` 事件并标记 `need_receiver_attention = true`。

4. OCR 失败兜底
   - 对卡片类图片执行本地 OCR。
   - OCR 失败时保留原图引用。
   - 生成 `ocr_status = failed` 事件并标记 `need_receiver_attention = true`。
   - 保证事件仍可正常上传。

5. 重试与容错
   - 本地维护上传失败重试队列。
   - 重试成功后更新状态。
   - 重试失败时继续保留失败事件并上报。

### 15.1.2 中转服务器任务

1. 事件接收
   - 接收 `POST /events`。
   - 校验事件字段是否完整。

2. 事件存储
   - 保存事件主表。
   - 保存图片引用。
   - 保存失败原因字段。

3. 去重与幂等
   - 依据 `event_id` 或 `message_key + checksum` 去重。
   - 重复事件不重复推送。

4. 推送策略
   - 仅对 `notify_receiver = true` 的事件推送。
   - 推送内容保持事件结构一致。

5. 处理回传
   - 接收 `POST /attention-status`。
   - 更新 `handled` 状态。

### 15.1.3 接收端任务

1. 事件展示
   - 普通图片显示图片。
   - 普通图片上传失败提示错误原因。
   - OCR 失败显示原图与失败提示。

2. 提醒机制
   - 对 `need_receiver_attention = true` 的事件主动提醒。
   - 明确显示“需要人工处理”。

3. 处理状态回传
   - 用户确认或处理后，发送 `handled` 状态给服务器。

### 15.1.4 验收标准

1. 普通图片成功转发时，接收端可见图片。
2. 普通图片上传失败时，接收端能看到失败提示。
3. OCR 失败时，接收端能看到原图，并收到提醒。
4. 重复事件不会重复推送。
5. 失败事件能够完整带上 `image_refs`、`error_code`、`error_message`。

## 15.2 第一阶段实现顺序表

| 阶段 | 模块 | 目标 | 交付物 | 验收点 |
| --- | --- | --- | --- | --- |
| 1 | 发射端消息采集 | 接入消息监听与消息抽取 | 统一消息对象 | 能拿到 `message_key`、`thread_key`、文本、图片引用 |
| 2 | 发射端图片分类 | 区分普通图片 / 卡片图片 / 未知图片 | 分类结果 | 未知图片进入保守 OCR 兜底 |
| 3 | 发射端普通图片处理 | 普通图片上传与失败处理 | 普通图片成功事件、失败事件 | 失败事件带 `error_code`、`error_message` |
| 4 | 发射端 OCR 兜底 | OCR 失败时保留原图并上报 | OCR 失败事件 | 接收端能看到原图与提醒 |
| 5 | 中转服务器事件接收 | 接收并校验事件 | `POST /events` | 登录后接口可接收并返回 accepted |
| 6 | 中转服务器存储 | 保存事件与图片引用 | 事件存储 | 事件主表、图片表落库成功 |
| 7 | 中转服务器去重 | 防止重复入库和重复推送 | 幂等逻辑 | 重复 `event_id` 不重复推送 |
| 8 | 中转服务器推送 | 推送需要接收端关注的事件 | 推送消息 | `notify_receiver = true` 的事件可推送 |
| 9 | 接收端展示 | 展示图片、失败提示、原图 | UI 展示 | 普通图片和 OCR 失败原图可见 |
| 10 | 接收端提醒 | 明确提醒人工处理 | 提醒交互 | `need_receiver_attention = true` 触发提醒 |
| 11 | 接收端回传 | 处理状态上报 | `POST /attention-status` | `handled` 状态可回传 |

## 15.3 第一阶段代码结构草案

> **说明**: 以下为 Python 参考实现目录结构。`source_app/` 和 `receiver_app/` 在 Android APK 版本中将被对应的 Kotlin/Java 代码替代，但业务逻辑和 JSON 协议完全一致。`relay_server/` 和 `shared/` 直接用于生产部署。

### 15.3.1 发射端（Python 参考 → Android APK）

```text
source_app/
├── app/
│   ├── main.py
│   ├── message_listener.py
│   ├── image_classifier.py
│   ├── normal_image_handler.py
│   ├── card_ocr_handler.py
│   ├── event_builder.py
│   ├── upload_client.py
│   ├── retry_queue.py
│   ├── local_storage.py
│   └── config.py
```

### 15.3.2 中转服务器（Python，直接部署）

```text
relay_server/
├── api/
│   ├── events.py
│   ├── attention_status.py
│   └── websocket.py
├── models/
│   ├── event.py
│   ├── image_ref.py
│   └── audit_log.py
├── services/
│   ├── event_service.py
│   ├── dedupe_service.py
│   ├── push_service.py
│   └── retry_service.py
├── storage/
│   ├── db.py
│   └── migrations/
└── config.py
```

### 15.3.3 接收端（Python 参考 → Android APK）

```text
receiver_app/
├── app/
│   ├── main.py
│   ├── event_receiver.py
│   ├── ui_renderer.py
│   ├── reminder_manager.py
│   ├── status_reporter.py
│   ├── image_loader.py
│   └── config.py
```

### 15.3.4 共享协议

```text
shared/
├── event_schema.py
├── error_codes.py
├── constants.py
└── api_client.py
```

### 15.3.5 代码职责汇总

- `message_listener.py`：拉取消息原始数据
- `image_classifier.py`：分类普通图片 / 卡片图片 / 未知图片
- `normal_image_handler.py`：普通图片事件生成、上传、失败处理
- `card_ocr_handler.py`：执行本地 OCR、失败兜底
- `event_builder.py`：统一构造事件对象
- `upload_client.py`：上传事件和图片引用
- `retry_queue.py`：维护失败重试队列
- `events.py`：接收发射端事件
- `dedupe_service.py`：去重与幂等
- `push_service.py`：推送到接收端
- `ui_renderer.py`：展示图片、文本、失败提示
- `reminder_manager.py`：提醒用户处理异常事件
- `status_reporter.py`：回传已处理状态

> 以上为 Python 参考实现的代码职责。Android APK 版本对应的实现映射见 §9.4 和 §11.3。

## 15.4 第一阶段字段与数据库表映射草案

### 15.4.1 数据库表设计

#### 表一：`events`

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint | 主键 |
| `event_id` | varchar | 事件唯一 ID |
| `event_type` | varchar | 事件类型 |
| `content_type` | varchar | 内容类型 |
| `source` | varchar | 消息来源 |
| `timestamp` | datetime | 事件时间 |
| `thread_key` | varchar | 会话键 |
| `message_key` | varchar | 消息键 |
| `summary` | text | 摘要 |
| `ocr_status` | varchar | OCR 状态 |
| `upload_status` | varchar | 上传状态 |
| `need_receiver_attention` | boolean | 是否需要提醒 |
| `notify_receiver` | boolean | 是否推送 |
| `checksum` | varchar | 校验值 |
| `content_text` | text | 文本内容 |
| `image_ocr_text` | text | OCR 文本 |
| `ocr_error` | varchar | OCR 错误码 |
| `error_code` | varchar | 上传错误码 |
| `error_message` | text | 上传错误信息 |
| `retry_count` | int | 重试次数 |
| `attention_status` | varchar | 处理状态 |
| `handled_at` | datetime | 处理完成时间 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

#### 表二：`event_images`

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint | 主键 |
| `event_id` | varchar | 关联事件 ID |
| `image_ref` | varchar | 图片引用 |
| `storage_uri` | varchar | 图片存储地址 |
| `checksum` | varchar | 图片校验值 |
| `created_at` | datetime | 创建时间 |

#### 表三：`event_delivery`

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| `id` | bigint | 主键 |
| `event_id` | varchar | 关联事件 ID |
| `receiver_id` | varchar | 接收端标识 |
| `push_status` | varchar | 推送状态 |
| `pushed_at` | datetime | 推送时间 |
| `delivered_at` | datetime | 接收时间 |
| `created_at` | datetime | 创建时间 |

### 15.4.2 字段映射关系

| 事件字段 | 数据库表 | 字段名 | 说明 |
| --- | --- | --- | --- |
| `event_id` | `events` | `event_id` | 主键之一 |
| `event_type` | `events` | `event_type` | 事件类型 |
| `content_type` | `events` | `content_type` | 内容类型 |
| `source` | `events` | `source` | 消息来源 |
| `timestamp` | `events` | `timestamp` | 事件时间 |
| `thread_key` | `events` | `thread_key` | 会话键 |
| `message_key` | `events` | `message_key` | 消息键 |
| `summary` | `events` | `summary` | 摘要 |
| `ocr_status` | `events` | `ocr_status` | OCR 状态 |
| `upload_status` | `events` | `upload_status` | 上传状态 |
| `need_receiver_attention` | `events` | `need_receiver_attention` | 是否提醒 |
| `notify_receiver` | `events` | `notify_receiver` | 是否推送 |
| `checksum` | `events` | `checksum` | 校验值 |
| `content_text` | `events` | `content_text` | 文本内容 |
| `image_ocr_text` | `events` | `image_ocr_text` | OCR 文本 |
| `ocr_error` | `events` | `ocr_error` | OCR 失败原因 |
| `error_code` | `events` | `error_code` | 错误码 |
| `error_message` | `events` | `error_message` | 错误信息 |
| `retry_count` | `events` | `retry_count` | 重试次数 |
| `attention_status` | `events` | `attention_status` | 处理状态 |
| `handled_at` | `events` | `handled_at` | 处理完成时间 |
| `image_refs` | `event_images` | `image_ref` | 单个图片引用 |
| `storage_uri` | `event_images` | `storage_uri` | 图片存储地址 |

### 15.4.3 事件落库规则

1. 事件入库时先写 `events` 表。
2. `image_refs` 数组逐条写入 `event_images` 表。
3. 若 `notify_receiver = true`，再写入 `event_delivery` 表。
4. 接收端回传 `handled` 后，更新 `events.attention_status` 与 `events.handled_at`。
5. 任何失败事件必须保留 `error_code`、`error_message` 或 `ocr_error`。

---

## 16. 文档维护记录

- 首次创建时间：2026-05-26
- 2026-05-26：第一阶段代码实现完成，补充第 17 章实现成果
- 2026-05-26：WebSocket 实时推送实现完成，补充第 17.8 节
- 2026-05-26：SQLite 替换 JSON 文件存储完成，补充第 17.9 节
- 2026-05-26：Tkinter GUI 接收端界面完成，补充第 17.10 节
- 2026-05-26：Tesseract 中文 OCR 集成完成，补充第 17.11 节
- 2026-05-26：ADB 实机消息监听集成完成，补充第 17.12 节
- 2026-05-26：GUI 系统托盘 + 气泡通知完成，补充第 17.13 节
- 2026-05-26：PyInstaller 打包部署完成，补充第 17.14 节
- 2026-05-27：完整版 vs fast 版中文包对比测试，补充第 17.11.6 节
- 2026-05-27：架构确认：发射端/接收端改为 Android APK，服务器保持 Python，补充第 17.15 节
- 2026-05-27：Android APK 开发完成（发射端 + 接收端），编译成功，补充第 19 章
- 2026-05-27：服务器部署至腾讯云 139.199.11.252 (HTTP:9006 + WS:9007)，补充第 20 章
- 当前状态：Android APK 已编译，服务器已上线运行

---

## 17. Python 参考实现成果（截至 2026-05-27）

> **说明**: 本章记录的 `source_app/` 和 `receiver_app/` 为 Python 参考实现，包含完整业务逻辑，可直接在 PC 运行以验证协议和数据流。`relay_server/` 直接用于生产部署。Android APK 版本需将 source_app 和 receiver_app 的逻辑映射为 Kotlin/Java 原生实现，详见第 17.15 节。

### 17.1 已完成的代码模块

#### 17.1.1 共享协议层 (`shared/`)

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| `event_schema.py` | 完成 | 事件字段校验 `validate_event()`、归一化 `normalize_event()`，含13个必填字段校验 |
| `error_codes.py` | 完成 | 5种错误码与文案映射，含 `get_error_message()` |
| `constants.py` | 完成 | 支持的内容类型/OCR状态/上传状态枚举 |
| `api_client.py` | 完成 | 标准库 HTTP POST 封装，返回统一 `{ok, status, body}` 格式 |

#### 17.1.2 发射端 (`source_app/`)

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| `config.py` | 完成 | 本地目录、`EVENT_API_URL`、`IMAGE_API_URL` 配置 |
| `local_storage.py` | 完成 | 图片二进制保存（sha256 去重命名）、文件复制、事件 JSON 缓存与清理 |
| `event_builder.py` | 完成 | 统一 `build_event()` 构造，字段默认值 + overrides 覆盖 |
| `upload_client.py` | 完成 | JSON 事件上传、multipart 图片上传、事件+图片联合上传、可重试错误判断 |
| `retry_queue.py` | 完成 | 指数退避（2^n 秒）、最大5次重试、死信队列、JSON 持久化、`get_queue_stats()` |
| `image_classifier.py` | 完成 | PIL 尺寸/宽高比三分法（竖屏→卡片、正方→普通、未知→保守兜底） |
| `card_ocr_handler.py` | 完成 | PaddleOCR→Tesseract→PIL 三层回退链、中英文状态识别、订单号/金额/时间正则提取、OCR 失败兜底事件 |
| `message_listener.py` | 完成 | 三种消息源模式（轮询/文件监听/ADB 桥接）、`MessageMeta` 元信息提取、统一 `on_message()` 入口 |
| `normal_image_handler.py` | 完成 | 普通图片上传链路、失败事件构建（含 `error_code`/`error_message`） |
| `main.py` | 完成 | `handle_incoming_message()` 主入口：分类→分派→上传，串联完整处理管线 |

#### 17.1.3 中转服务器 (`relay_server/`)

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| `demo_server.py` | 完成 | 全功能 HTTP 服务器，8 个端点（见 17.2 节） |
| `storage/db.py` | 完成 | SQLite 连接管理（WAL 模式、外键约束）、4 表 DDL、线程本地连接池、`get_store_stats()` |
| `storage/local_store.py` | 完成 | **SQLite 实现**：15 个函数全部替换为参数化 SQL，完全兼容旧接口 |
| `api/events.py` | 完成 | `POST /events`：校验→去重→入库→图片关联→WS 推送 |
| `api/attention_status.py` | 完成 | `POST /attention-status`：状态回传→标记 handled |
| `services/event_service.py` | 完成 | 事件创建、图片引用保存、状态更新 |
| `services/dedupe_service.py` | 完成 | `event_id` + `checksum` 双重去重 |
| `services/push_service.py` | 完成 | WebSocket 广播优先，HTTP 队列轮询回退，返回 `ws_delivered` 状态 |
| `ws_server.py` | 完成 | WebSocket 服务器（端口 8001），线程安全广播，发送队列解耦，心跳检测 |

#### 17.1.4 接收端 (`receiver_app/`)

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| `config.py` | 完成 | 消费策略（可配置轮询间隔/超时/重试）、环境变量覆盖、JSON 配置文件 |
| `event_receiver.py` | 完成 | **双模式**: `connect_ws()` WebSocket 实时接收（默认），`connect_push()` HTTP 轮询回退（`--poll`），ACK 确认含重试 |
| `ui_renderer.py` | 完成 | 四类渲染：OCR 失败/上传失败/图片卡片/文本，格式化终端输出 |
| `reminder_manager.py` | 完成 | `need_receiver_attention` 触发提醒 |
| `status_reporter.py` | 完成 | `POST /attention-status` 处理回传 |
| `image_loader.py` | 完成 | 本地文件/URL 下载缓存/base64 解码、PIL 图片信息提取、缩略图生成 |
| `main.py` | 完成 | 三种启动模式：默认 WebSocket，`--gui` Tkinter 界面，`--poll` HTTP 轮询 |
| `gui.py` | 完成 | Tkinter GUI：左侧事件列表（需关注红色高亮）、右侧详情面板、图片预览、标记已处理、WebSocket 后台线程 |

### 17.2 中转服务器端点清单

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/health` | 健康检查（含 `ws_clients` 在线数） |
| GET | `/stats` | 数据库统计（总事件数、待投递数、图片数、已处理数） |
| GET | `/ws/status` | WebSocket 服务状态（端口、客户端数） |
| GET | `/events/pending` | 获取所有待消费事件 |
| GET | `/events/next` | 出队下一条待消费事件 |
| GET | `/images/<filename>` | 获取已上传图片（含 MIME 类型、缓存头） |
| POST | `/events` | 接收事件（校验→去重→存储→推送），响应含 `ws_delivered` 状态 |
| POST | `/events/ack` | 确认消费完成 |
| POST | `/images` | 上传图片（multipart/form-data，10MB 限制） |
| POST | `/attention-status` | 处理状态回传 |
| **WS** | `ws://127.0.0.1:8001` | WebSocket 实时推送（见 17.8 节） |

### 17.3 OCR 引擎集成

| 引擎 | 状态 | 说明 |
| --- | --- | --- |
| PaddleOCR 3.5.0 | 已安装 | 模型已下载（PP-OCRv5_server），Windows oneDNN 兼容性问题待修复，已纳入 RuntimeError 回退链 |
| Tesseract | 已安装 | `C:\Program Files\Tesseract-OCR\`，英文 + 中文（chi_sim_fast）双语言包 |
| PIL 回退 | 已集成 | 读取图片尺寸/格式作为保底输出 |

**三层回退链**: PaddleOCR → Tesseract（含图像预处理） → PIL 保底

**图像预处理**: 灰度转换 + 2倍对比度增强，解决红色/彩色文字识别率低的问题

**中文 OCR 实测结果** (闲鱼交易卡片, Tesseract chi_sim_fast):

| 字段 | 识别结果 | 状态 |
| --- | --- | --- |
| 状态 | `paid`（原文"买家已付款"） | 通过 |
| 订单号 | `order_2026052615005678` | 通过 |
| 金额 | `1299.00`（原文"1299.00元"） | 通过 |
| 时间 | `2026年5月26日 15:30` | 通过 |

**中文包推荐**: `chi_sim_fast.traineddata` (2.5MB)，实测识别率优于完整版 (42MB)。完整版会在中文字间插入多余空格降低准确率。

### 17.4 端到端测试覆盖

| 测试场景 | 检查项 | 结果 |
| --- | --- | --- |
| 普通图片事件 | accepted、不入推送队列 | 通过 |
| 上传失败事件 | accepted、error_code/error_message 完整、入推送队列 | 通过 |
| OCR 失败事件 | accepted、ocr_error 完整、原图引用保留、入推送队列 | 通过 |
| 推送队列过滤 | notify_receiver=false 不入队列 | 通过 |
| 队列消费 | 按序出队、消费完队列为空 | 通过 |
| ACK 确认 | acked=true、队列移除 | 通过 |
| 去重 | 重复 event_id 返回 duplicate | 通过 |
| 事件校验 | 不完整事件返回 rejected + missing_fields | 通过 |
| 状态回传 | attention_status=handled 写回 | 通过 |
| 重试队列 | 入队、指数退避、死信队列 | 通过 |
| 图片上传 | multipart 上传、URL 返回、checksum 校验 | 通过 |
| 图片获取 | MIME 类型正确、路径遍历拦截（403） | 通过 |
| OCR 真实识别 | Tesseract 英文→状态/订单号/金额/时间 结构化解 | 通过 |

### 17.5 运行方式

```powershell
# 1. 安装依赖
pip install -r requirements.txt
pip install websockets    # WebSocket 实时推送

# 2. 启动中转服务器（HTTP + WebSocket 双端口）
python relay_server/demo_server.py
# 输出: HTTP 服务: http://127.0.0.1:8000
#       WebSocket 服务: ws://127.0.0.1:8001

# 3. 启动接收端（WebSocket 实时模式，默认）
python receiver_app/app/main.py

# 3b. 启动接收端（HTTP 轮询回退模式）
python receiver_app/app/main.py --poll

# 4. 运行全流程演示（含 WebSocket 实时推送）
python demo.py

# 5. 运行集成测试
python tests/test_e2e.py

# 6. 运行 OCR 测试
python tests/test_ocr.py

# 7. 运行 WebSocket 测试
python tests/test_websocket.py
```

### 17.6 验收标准对照

| # | 验收标准 | 状态 |
| --- | --- | --- |
| 1 | 普通图片成功转发时，接收端可见图片 | 通过 |
| 2 | 普通图片上传失败时，接收端能看到失败提示 | 通过 |
| 3 | OCR 失败时，接收端能看到原图，并收到提醒 | 通过 |
| 4 | 重复事件不会重复推送 | 通过 |
| 5 | 失败事件能够完整带上 `image_refs`、`error_code`、`error_message` | 通过 |

### 17.7 下一阶段建议

| 方向 | 说明 | 工作量 |
| --- | --- | --- |
| ~~Tesseract 中文包~~ | ✅ 已完成（详见 17.11 节） | - |
| ~~WebSocket 实时推送~~ | ✅ 已完成（详见 17.8 节） | - |
| ~~SQLite 替换 JSON~~ | ✅ 已完成（详见 17.9 节） | - |
| ~~接收端 GUI (Python)~~ | ✅ 已完成（详见 17.10 节） | - |
| 发射端 Android APK | `NotificationListenerService` + Tesseract/ML Kit + OkHttp | 大 |
| 接收端 Android APK | OkHttp WebSocket + RecyclerView + Glide + NotificationManager | 大 |
| PaddleOCR Windows 修复 | 等待 PaddlePaddle 修复 oneDNN PIR 兼容性 | 小 |

### 17.8 WebSocket 实时推送实现

#### 17.8.1 架构

```
发射端 → POST /events → 中转服务器 → notify_receiver=true
                                         ├─ WebSocket 实时广播 (ws://8001) → 接收端即时收到
                                         └─ HTTP 队列轮询 /events/next (回退)
```

#### 17.8.2 新增/修改文件

| 文件 | 变更 | 说明 |
| --- | --- | --- |
| `relay_server/ws_server.py` | **新增** | WebSocket 服务器核心。独立 asyncio 事件循环线程，`_ws_connections` 连接集合管理，`_send_queue` 线程安全队列解耦，`broadcast_event()` 非阻塞广播 |
| `relay_server/services/push_service.py` | **重写** | `dispatch_event()` 优先调用 `broadcast_event()` 实时推送，返回 `ws_clients` 和 `ws_delivered` 状态 |
| `relay_server/api/events.py` | **修改** | `create_event()` 末尾调用 `PUSH_SERVICE.dispatch_event()`，响应体包含推送状态 |
| `relay_server/demo_server.py` | **修改** | `main()` 启动时调用 `start_ws_server()` 启动 WS 线程，`/health` 返回 `ws_clients`，新增 `/ws/status` 端点 |
| `receiver_app/app/event_receiver.py` | **重写** | 新增 `connect_ws()` 异步 WebSocket 消费（默认），保留 `connect_push()` HTTP 轮询（`--poll`） |
| `receiver_app/app/config.py` | **修改** | 新增 `WS_URL = "ws://127.0.0.1:8001"` |
| `receiver_app/app/main.py` | **修改** | 默认调用 `connect_ws()`，`--poll` 参数切换 `connect_push()` |

#### 17.8.3 WebSocket 协议

**服务端→客户端:**

| type | 说明 | 示例 |
| --- | --- | --- |
| `connected` | 连接确认，含 `client_id` | `{"type":"connected","client_id":"client_xxx","message":"已连接到中转服务器"}` |
| `event` | 实时事件推送 | `{"type":"event","event":{...完整事件对象...}}` |
| `ping` | 心跳（30s间隔） | `{"type":"ping","ts":1779799475}` |
| `ack_ok` | ACK 确认 | `{"type":"ack_ok","event_id":"evt_002"}` |

**客户端→服务端:**

| type | 说明 |
| --- | --- |
| `pong` | 心跳响应 |
| `ack` | 确认消费（`{"type":"ack","event_id":"evt_002"}`） |

#### 17.8.4 关键设计决策

- **线程安全**: HTTP 线程通过 `queue.Queue` 将事件放入发送队列，WS 事件循环线程消费队列并广播
- **连接管理**: `_ws_connections` set 仅在事件循环线程内访问，无需锁
- **客户端计数**: `_client_count` 通过 `threading.Lock` 保护，供 HTTP 端健康检查使用
- **断线重连**: 接收端自动重连，指数退避（1s→2s→4s→...→30s 上限）
- **回退兼容**: WebSocket 不可用时自动降级到 HTTP 轮询模式

#### 17.8.5 测试验证

| 测试场景 | 结果 |
| --- | --- |
| WS 客户端连接成功，收到 `connected` 消息 | 通过 |
| 普通图片 (`notify_receiver=false`) 不通过 WS 推送 | 通过 |
| 上传失败事件 (`notify_receiver=true`) 实时推送到 WS 客户端 | 通过 |
| OCR 失败事件 (`notify_receiver=true`) 实时推送到 WS 客户端 | 通过 |
| WS 客户端断开后 `ws_clients` 计数归零 | 通过 |
| `/ws/status` 端点返回正确的 WS 服务状态 | 通过 |

### 17.9 SQLite 存储实现

#### 17.9.1 数据库表结构

按照文档 15.4 节设计，实现 4 张表：

| 表名 | 说明 | 关键字段 |
| --- | --- | --- |
| `events` | 事件主表 | event_id (UNIQUE), 18 个业务字段, created_at, updated_at |
| `event_images` | 图片引用 | event_id (FK), image_ref, storage_uri, checksum |
| `event_delivery` | 投递记录 | event_id, push_status (pending/inflight/delivered), pushed_at, delivered_at |
| `image_index` | 上传图片索引 | checksum (PK), url, local_path, stored_at |

#### 17.9.2 修改文件

| 文件 | 变更 | 说明 |
| --- | --- | --- |
| `relay_server/storage/db.py` | **重写** | SQLite 连接管理：WAL 模式、外键约束、线程本地连接池（`threading.local()`）、`init_db()` 幂等建表、`get_db()` 自动初始化 |
| `relay_server/storage/local_store.py` | **重写** | 全部 18 个函数替换为参数化 SQL 查询，函数签名完全兼容旧 JSON 版本 |
| `relay_server/config.py` | **修改** | 移除不再使用的 `EVENTS_PATH`、`HANDLED_PATH` |
| `relay_server/demo_server.py` | **修改** | 启动时调用 `init_db()`，新增 `GET /stats` 端点 |

#### 17.9.3 技术要点

- **线程安全**: 每线程一个 `sqlite3.Connection`，通过 `threading.get_ident()` 区分
- **WAL 模式**: 读写并发，写入不阻塞读取
- **参数化查询**: 所有 SQL 使用 `?` 占位符，防止 SQL 注入
- **幂等建表**: `CREATE TABLE IF NOT EXISTS`，重复调用安全
- **兼容旧接口**: 函数签名与 JSON 版本完全一致，调用方无感知
- **in-flight 超时回收**: `dequeue_event()` 中自动将 >300s 的 inflight 重置为 pending
- **统计端点**: `GET /stats` 返回 `{total_events, pending_delivery, total_images, handled}`

#### 17.9.4 验证结果

| 测试项 | 结果 |
| --- | --- |
| 集成测试 20/20 | 通过 |
| WebSocket 测试 6/6 | 通过 |
| `/stats` 统计准确（5事件→2待投递→1已处理） | 通过 |
| 数据库文件 (`events.db`) 正常生成（4KB/5事件） | 通过 |

### 17.10 Python 参考：Tkinter GUI 接收端界面

#### 17.10.1 布局设计

```
┌──────────────────────────────────────────────────┐
│  闲鱼消息转发 - 接收端                            │
├─────────────────────┬────────────────────────────┤
│  事件列表            │  事件详情                    │
│                     │                            │
│  [!] gui_002 上传失败 │  event_type: image          │
│  [!] gui_003 OCR失败  │  summary: 图片上传超时       │
│      gui_001 普通图片  │  >>> 错误码: upload_timeout  │
│                     │  >>> 错误信息: ...            │
│                     │                            │
│                     │  [图片预览]                   │
│                     │                            │
│                     │  [标记已处理]  需要人工处理！   │
│                     │              已连接 WS       │
├─────────────────────┴────────────────────────────┤
│  已连接 ws://127.0.0.1:8001         事件: 3      │
└──────────────────────────────────────────────────┘
```

#### 17.10.2 功能特性

| 功能 | 说明 |
| --- | --- |
| 实时事件列表 | WebSocket 后台线程接收，100ms 定时刷新 UI |
| 需要关注高亮 | `need_receiver_attention=true` 且未处理的事件以红色背景标记 `[!]` |
| 事件详情 | 点击事件显示完整字段：event_id, summary, error_code, error_message, ocr_error, OCR 文本 |
| 图片预览 | 自动加载 `image_refs` 第一张图，显示缩略图（URL 下载缓存） |
| 标记已处理 | 按钮调用 `POST /attention-status`，本地标记 `_handled`，列表刷新 |
| 连接状态 | 右下角显示 WebSocket 连接状态 |
| 断线重连 | WebSocket 断开自动重连，指数退避（1s→30s） |

#### 17.10.3 技术实现

| 组件 | 方案 |
| --- | --- |
| GUI 框架 | Tkinter（Python 标准库，零依赖） |
| 实时数据 | WebSocket 线程 → `queue.Queue` → 主线程 `after(100ms)` 轮询刷新 |
| 线程安全 | `EventStore` 类使用 `threading.Lock` 保护事件字典 |
| 图片显示 | PIL `ImageTk.PhotoImage` + 缩略图（`load_thumbnail(max_size=280)`） |
| 布局 | `ttk.PanedWindow` 可拖拽分割面板 |

#### 17.10.4 新增/修改文件

| 文件 | 变更 | 说明 |
| --- | --- | --- |
| `receiver_app/app/gui.py` | **新增** | ~260 行，`ReceiverGUI` 类（含 `EventStore`） |
| `receiver_app/app/main.py` | **修改** | 新增 `--gui` 启动参数 |

#### 17.10.5 运行方式

```powershell
# 1. 启动中转服务器
python relay_server/demo_server.py

# 2. 启动 GUI 接收端
python receiver_app/app/main.py --gui
```

#### 17.10.6 验证结果

| 测试项 | 结果 |
| --- | --- |
| GUI 模块导入无错误 | 通过 |
| EventStore 线程安全（add/mark_handled/get_all） | 通过 |
| WebSocket 实时接收 2 条事件（普通图片不推送） | 通过 |
| 需关注事件红色高亮标记 | 通过 |
| 标记已处理更新状态 | 通过 |

### 17.11 Tesseract 中文 OCR 集成

#### 17.11.1 中文包安装

- 下载 `chi_sim_fast.traineddata`（2.5MB，快速版）到项目 `data/tessdata/` 目录
- 通过 `--tessdata-dir` 命令行参数加载，无需管理员权限
- 完整版 `chi_sim.traineddata`（42MB）可从 GitHub tessdata 仓库下载

#### 17.11.2 修改文件

| 文件 | 变更 | 说明 |
| --- | --- | --- |
| `source_app/app/card_ocr_handler.py` | **修改** | Tesseract 改为 `subprocess` 命令行调用（避免 pytesseract 路径编码问题）；新增 `_preprocess_for_ocr()` 图像预处理（灰度+2倍对比度）；金额正则修复（支持空格小数点 + RMB 前缀 + group3 捕获） |
| `data/tessdata/chi_sim.traineddata` | **新增** | 中文语言包（2.5MB） |

#### 17.11.3 图像预处理

```
原图 (彩色) → PIL convert("L") 灰度 → ImageEnhance.Contrast 2倍 → Tesseract OCR
```

效果：红色文字"支付时间: 2026年5月26日 15:30"在预处理前无法识别，预处理后准确识别。

#### 17.11.4 OCR 三层回退链

```
PaddleOCR (chi_sim, PP-OCRv5) → Tesseract (chi_sim+eng, subprocess) → PIL 保底
         ↓ RuntimeError                    ↓ RuntimeError                ↓
    oneDNN 兼容性问题               Tesseract 不可用               图片基本信息
```

#### 17.11.5 中文 OCR 实测结果

| 字段 | OCR 原文 | 解析结果 | 状态 |
| --- | --- | --- | --- |
| 状态 | 交易状态: 买家已付款 | `paid` | 通过 |
| 订单号 | 订单编号: order_2026052615005678 | `order_2026052615005678` | 通过 |
| 金额 | 商品价格: 1299.00元 | `1299.00` | 通过 |
| 时间 | 支付时间: 2026年5月26日 15:30 | `2026年5月26日 15:30` | 通过 |
| 地址 | 浙江省杭州市余杭区五常街道 | - | 通过 |
| 中文字符 | 84 个 | - | 通过 |

#### 17.11.6 完整版 vs fast 版对比（2026-05-27）

| 对比项 | fast 版 (2.5MB) | 完整版 (43MB) |
| --- | --- | --- |
| 状态 | `paid` ✓ | `paid` ✓（需空格清理） |
| 订单号 | `order_...` ✓ | 失败（字符间空格过多） |
| 金额 | `1299.00` ✓ | `1299.00` ✓ |
| 时间 | `2026年5月26日...` ✓ | 失败（字符识别不准） |
| 中文字符 | 84 个 ✓ | 81 个 |
| **总通过率** | **4/4** | **2/4** |

**结论**: fast 版识别率优于完整版。完整版因在中文间插入多余空格而降低解析准确率。项目中 `parse_ocr_result` 已加入中文空格自动清理逻辑，但仍推荐使用 fast 版以获得最佳效果。

### 17.12 Python 参考：ADB 消息监听

#### 17.12.1 架构

```
Android 手机 (闲鱼 App)
    ↓ USB/WiFi ADB
adb shell dumpsys notification --noredact
    ↓ 每 2s 轮询
message_listener._parse_notifications()
    ↓ 按 pkg 过滤 (com.taobao.idlefish)
    ↓ 去重 (notification key)
    ↓ 提取 title / text / bigText / image_uris
    ↓ on_message() 标准化
    ↓ handle_incoming_message()
    ↓ 上传到中转服务器
```

#### 17.12.2 修改文件

| 文件 | 变更 | 说明 |
| --- | --- | --- |
| `source_app/app/message_listener.py` | **重写** | 三种消息源模式（ADB/文件/轮询），`_get_adb()` 自动搜索 ADB 路径，`_parse_notifications()` 生产级 dumpsys 解析，500 条去重窗口，设备断开自动重连 |
| `source_app/app/main.py` | **重写** | 默认 ADB 模式启动，`--test` 单次模拟测试，`handle_incoming_message` 自动生成 event_id + checksum |

#### 17.12.3 ADB 环境检测

自动搜索以下路径的 ADB:
1. `adb` (PATH)
2. `%LOCALAPPDATA%/Android/Sdk/platform-tools/adb.exe`
3. `C:/Users/mdjyx/AppData/Local/Android/Sdk/platform-tools/adb.exe`
4. `C:/platform-tools/adb.exe`

ADB 不可用时自动回退到文件监听模式。

#### 17.12.4 dumpsys 通知解析能力

| 提取字段 | 正则/方法 | 说明 |
| --- | --- | --- |
| 包名 | `pkg=([^\s\n,}]+)` | 用于过滤非闲鱼通知 |
| 通知 key | `key=([^\s\n)]+)` | 去重依据，缓存 500 条，超出保留最近 200 |
| 标题 | `android.title=([^\n]+)` | 通知标题 |
| 内容 | `android.text=([^\n]+)` | 通知正文 |
| 扩展内容 | `android.bigText=([^\n]+)` | 展开后的大文本 |
| 时间 | `when=(\d+)` | Unix 毫秒时间戳 |
| 图片 URI | `uri=([^\s\n]+\.(?:jpg\|jpeg\|png\|webp))` | 通知附带的图片引用 |

#### 17.12.5 验证结果

| 测试项 | 结果 |
| --- | --- |
| ADB 环境检测 (1.0.41) | 通过 |
| 3 条闲鱼 + 1 条微信 → 仅提取 3 条闲鱼 | 通过 |
| 消息内容完整（标题+正文+扩展文本） | 通过 |
| 重复通知过滤（二次解析返回 0） | 通过 |
| 消息标准化 (source=notification_bar) | 通过 |
| 端到端上传 (3/3 事件入库) | 通过 |

### 17.13 Python 参考：GUI 系统托盘 + 气泡通知

#### 17.13.1 功能特性

| 功能 | 说明 |
| --- | --- |
| 最小化到托盘 | 关闭窗口时隐藏到系统托盘而非退出 |
| 右键菜单 | 显示窗口 / 退出 |
| 托盘图标 | 橙色圆形图标（PIL 生成） |
| Windows Toast 通知 | `need_receiver_attention=true` 事件到达时弹出原生通知 |
| 自动重连 | WebSocket 断开自动重连，状态栏 + 托盘提示 |

#### 17.13.2 修改文件

| 文件 | 变更 | 说明 |
| --- | --- | --- |
| `receiver_app/app/gui.py` | **重写** | `_create_tray_icon()` pystray 托盘图标，`_show_toast()` PowerShell Windows Toast，窗口关闭最小化到托盘 |
| `requirements.txt` | **修改** | 新增 `pystray>=0.19` |

#### 17.13.3 技术实现

| 组件 | 方案 |
| --- | --- |
| 托盘框架 | `pystray` + PIL 图标生成 |
| 气泡通知 | PowerShell 调用 `Windows.UI.Notifications.ToastNotificationManager`（零额外依赖） |
| 通知触发 | `_poll_queue()` 检测到 `need_receiver_attention=true` 事件时调用 `show_attention_notification()` |
| 图标运行 | `pystray.Icon.run()` 在独立 daemon 线程中运行 |

#### 17.13.4 运行

```powershell
# 启动 GUI（含托盘）
python receiver_app/app/main.py --gui

# 关闭窗口 → 最小化到托盘
# 右键托盘图标 → 显示窗口 / 退出
# 新事件到达 → Windows 气泡通知
```

---

## 18. 项目文件清单

### 18.1 完整文件树

```text
通知栏转发2/
├── requirements.txt              # Python 依赖清单
├── demo.py                       # 全流程演示脚本
├── 运行说明.md                    # 运行说明
├── 项目方案与落地文档.md          # 本文档
├── shared/                       # 共享协议层
│   ├── __init__.py
│   ├── event_schema.py           # 事件校验与归一化
│   ├── error_codes.py            # 错误码映射
│   ├── constants.py              # 常量定义
│   └── api_client.py             # HTTP 客户端封装
├── source_app/                   # 发射端
│   └── app/
│       ├── __init__.py
│       ├── main.py               # 启动入口 (ADB/文件/轮询)
│       ├── config.py             # 发射端配置
│       ├── message_listener.py   # 消息监听 (ADB/文件/轮询)
│       ├── image_classifier.py   # 图片分类器
│       ├── event_builder.py      # 事件构造器
│       ├── normal_image_handler.py # 普通图片处理
│       ├── card_ocr_handler.py   # OCR 处理 (PaddleOCR/Tesseract/PIL)
│       ├── upload_client.py      # 上传客户端
│       ├── retry_queue.py        # 重试队列
│       └── local_storage.py      # 本地存储
├── relay_server/                 # 中转服务器
│   ├── __init__.py
│   ├── config.py                 # 服务器配置
│   ├── demo_server.py            # HTTP + WebSocket 主服务
│   ├── ws_server.py              # WebSocket 推送服务
│   ├── api/
│   │   ├── __init__.py
│   │   ├── events.py             # POST /events
│   │   ├── attention_status.py   # POST /attention-status
│   │   └── websocket.py          # WebSocket 推送(占位)
│   ├── models/
│   │   ├── __init__.py
│   │   ├── event.py              # 事件模型
│   │   ├── image_ref.py          # 图片引用模型
│   │   └── audit_log.py          # 审计日志模型
│   ├── services/
│   │   ├── __init__.py
│   │   ├── event_service.py      # 事件存储服务
│   │   ├── dedupe_service.py     # 去重服务
│   │   ├── push_service.py       # 推送服务 (WS+队列)
│   │   └── retry_service.py      # 重试服务
│   └── storage/
│       ├── __init__.py
│       ├── db.py                 # SQLite 数据库
│       └── local_store.py        # 持久化存储 (SQLite)
├── receiver_app/                 # 接收端
│   └── app/
│       ├── __init__.py
│       ├── main.py               # 启动入口 (WS/GUI/轮询)
│       ├── config.py             # 接收端配置
│       ├── gui.py                # Tkinter GUI (托盘+通知)
│       ├── event_receiver.py     # 事件接收 (WS+轮询)
│       ├── ui_renderer.py        # 终端展示渲染
│       ├── reminder_manager.py   # 提醒管理
│       ├── status_reporter.py    # 状态回传
│       └── image_loader.py       # 图片加载器
├── tests/                        # 测试
│   ├── test_e2e.py               # 集成测试 (20项)
│   ├── test_websocket.py         # WebSocket 测试 (6项)
│   ├── test_ocr.py               # OCR 测试
│   ├── test_ocr_chinese.py       # 中文 OCR 测试
│   ├── test_gui.py               # GUI 测试 (6项)
│   └── test_adb.py               # ADB 解析测试 (10项)
└── data/
    ├── tessdata/                 # Tesseract 中文语言包
    │   └── chi_sim.traineddata
    ├── test_images/              # 测试图片
    └── watch/                    # 文件监听目录
```

### 18.2 功能实现总览

> 所有功能在 Python 参考实现中已通过测试。标记 `*` 的模块在 Android APK 版本中需重新实现，业务逻辑不变。

| 阶段 | 功能 | 状态 | 测试 |
|------|------|------|------|
| P0 基础 | 共享协议 (事件校验/归一化/API客户端) | ✅ | 20/20 |
| P0 基础 | 发射端事件构建 + 上传 + 重试 | ✅ | 20/20 |
| P0 基础 | 中转服务器 HTTP API + 存储 + 去重 | ✅ 生产可用 | 20/20 |
| P0 基础 | 接收端事件消费 + 展示 + 提醒 + 回传 | ✅ | 20/20 |
| P1 核心 | 消息监听 (Python: ADB → Android: NotificationListener) | ✅ | 10/10 |
| P1 核心 | 图片分类 (三分法) | ✅ | - |
| P1 核心 | 普通图片处理 + 上传失败兜底 | ✅ | 20/20 |
| P1 核心 | OCR 处理 (Python: PaddleOCR/Tesseract → Android: tess-two/ML Kit) | ✅ | 4/4 |
| 增强 | 图片上传端点 (multipart + 安全) | ✅ 生产可用 | - |
| 增强 | WebSocket 实时推送 | ✅ 生产可用 | 6/6 |
| 增强 | SQLite 替换 JSON 存储 | ✅ 生产可用 | 20/20 |
| 增强 | GUI + 托盘 + 通知 (Python 参考 → Android NotificationManager) | ✅ | 6/6 |

### 18.3 技术栈

| 层级 | Python 参考实现 | 生产目标 |
|------|---------------|----------|
| 发射端 | Python + ADB | Android APK (NotificationListenerService + Tesseract/ML Kit + OkHttp) |
| 中转服务器 | Python + SQLite | **同左，直接部署** (http.server + websockets) |
| 接收端 | Python + Tkinter | Android APK (OkHttp WS + RecyclerView + Glide + NotificationManager) |
| OCR | PaddleOCR → Tesseract → PIL | Android Tesseract `tess-two` / ML Kit |
| 存储 | SQLite (WAL) | 同左 |
| 推送 | WebSocket (ws://8001) | 同左 |

### 18.4 端口与服务

| 端口 | 协议 | 说明 |
|------|------|------|
| 8000 | HTTP | 中转服务器 REST API |
| 8001 | WebSocket | 事件实时推送 |

### 18.5 快速启动

**生产部署（服务器 + Android APK）:**

```bash
# 服务器端（Linux VPS）
pip install -r requirements.txt websockets
python relay_server/demo_server.py
# 监听 0.0.0.0:8000 (HTTP) + 0.0.0.0:8001 (WebSocket)
```

```kotlin
// 发射端 APK（Android）
// NotificationListenerService → 过滤闲鱼通知 → OCR → POST /events → 服务器

// 接收端 APK（Android）
// OkHttp WebSocket → ws://服务器IP:8001 → RecyclerView 展示 → 标记已处理
```

**Python 参考实现（PC 验证用）:**

```powershell
# 终端 1: 中转服务器
python relay_server/demo_server.py

# 终端 2: 接收端 GUI (Python 参考)
python receiver_app/app/main.py --gui

# 终端 3: 发射端 (Python 参考)
python source_app/app/main.py --file
```

### 18.6 PyInstaller 打包（Python 参考实现）

> **说明**: 仅 `server.exe` 用于生产部署。`receiver.exe` 和 `source.exe` 为 Python 参考实现，对应功能在 Android APK 中重新实现。

| 程序 | 大小 | 说明 |
|------|------|------|
| `dist/server/server.exe` | 66MB | 中转服务器 — **生产可用** (HTTP+WS+SQLite) |
| `dist/receiver/receiver.exe` | 74MB | 接收端 GUI — Python 参考 (Tkinter+托盘+通知) |
| `dist/source/source.exe` | 21MB | 发射端 — Python 参考 (ADB 模式) |

**重新打包:** `python build.py` 一键打包三端，或 `python build.py server|receiver|source` 单独打包。

---

*文档维护完成。方案设计 → 代码实现 → 测试覆盖 → 打包分发，全链路闭环。*

---

## 19. Android APK 实现成果（2026-05-27）

### 19.1 项目结构

```
通知栏转发2/android/
├── build.gradle.kts                 # 根构建 (AGP 8.7.3 + Kotlin 2.0.21)
├── settings.gradle.kts              # shared, source-app, receiver-app
├── gradle/libs.versions.toml        # 版本目录
├── shared/                          # 共享协议库 (10个源文件)
│   └── src/main/kotlin/com/idlefish/relay/shared/
│       ├── model/                   # EventRecord, ErrorCodes, AttentionStatus
│       ├── schema/                  # EventValidator, Constants
│       ├── network/                 # ApiEndpoints, ServerConfig, ApiResponse
│       └── util/                    # ChecksumUtil, TimestampUtil
├── source-app/                      # 发射端 APK (16个Kotlin源文件)
│   └── src/main/kotlin/com/idlefish/relay/source/
│       ├── service/NotificationMonitorService.kt  # 通知监听（过滤idlefish）
│       ├── parser/NotificationParser.kt           # 通知解析
│       ├── classifier/ImageClassifier.kt          # Bitmap宽高比三分法
│       ├── ocr/TesseractOcrEngine.kt              # OCR引擎（接口+桩实现）
│       ├── ocr/OcrResultParser.kt                 # 正则提取订单号/金额/状态
│       ├── handler/EventBuilder.kt                # 事件构造器（5种事件）
│       ├── handler/MessageDispatcher.kt           # 消息分发主入口
│       ├── handler/CardOcrHandler.kt              # OCR卡片处理
│       ├── handler/NormalImageHandler.kt          # 普通图片处理
│       ├── upload/EventUploader.kt                # POST /events
│       ├── upload/ImageUploader.kt                # POST /images (multipart)
│       ├── upload/HttpClient.kt                   # OkHttp单例
│       ├── data/                                  # Room数据库 (重试队列+死信)
│       ├── retry/RetryWorker.kt                   # WorkManager周期重试
│       ├── retry/RetryRepository.kt              # 重试队列操作
│       └── ui/SetupActivity.kt + SettingsActivity.kt
└── receiver-app/                    # 接收端 APK (10个Kotlin源文件)
    └── src/main/kotlin/com/idlefish/relay/receiver/
        ├── service/WebSocketService.kt            # 前台Service维持WS长连接
        ├── network/WebSocketManager.kt            # OkHttp WS + 断线重连(1s→30s)
        ├── data/EventRepository.kt                # 内存事件存储 + StateFlow
        ├── viewmodel/EventViewModel.kt            # MVVM ViewModel
        ├── notification/AttentionNotifier.kt      # NotificationManager系统通知
        ├── ui/MainActivity.kt                     # 单Activity + Fragment
        ├── ui/EventListFragment.kt                # RecyclerView事件列表
        ├── ui/EventDetailFragment.kt              # 详情 + 图片 + 标记已处理
        └── ui/adapter/EventAdapter.kt             # 红色[!]高亮需关注事件
```

### 19.2 技术栈

| 层级 | 技术 |
|------|------|
| 语言 | Kotlin |
| 最低SDK | 26 (Android 8.0) |
| 架构 | MVVM (ViewModel + StateFlow) |
| 序列化 | org.json.JSONObject |
| 网络 | OkHttp 4.12 (HTTP + WebSocket) |
| 本地存储 | Room 2.6 (重试队列) |
| 图片加载 | Glide 4.16 (接收端) |
| 后台任务 | WorkManager 2.9 (发射端) |

### 19.3 关键映射（Python → Android）

| Python 参考 | Android 实现 |
|------------|-------------|
| `message_listener.py` (ADB dumpsys) | `NotificationMonitorService` (系统回调) |
| `image_classifier.py` | `ImageClassifier` (Bitmap宽高比) |
| `card_ocr_handler.py` → Tesseract CLI | `TesseractOcrEngine` + `CardOcrHandler` |
| `event_builder.py` | `EventBuilder` |
| `upload_client.py` (urllib) | `EventUploader` + `ImageUploader` (OkHttp) |
| `retry_queue.py` (JSON文件) | `RetryWorker` + `RetryRepository` (Room) |
| `event_receiver.py` (websockets) | `WebSocketManager` (OkHttp WS) |
| `gui.py` (Tkinter) | `EventListFragment` + `EventDetailFragment` |
| `reminder_manager.py` (print) | `AttentionNotifier` (NotificationManager) |
| `status_reporter.py` | `EventDetailFragment.markAsHandled()` |

### 19.4 编译产物

| APK | 大小 | 路径 |
|-----|------|------|
| 发射端 | 5.7 MB | `android/source-app/build/outputs/apk/debug/source-app-debug.apk` |
| 接收端 | 5.3 MB | `android/receiver-app/build/outputs/apk/debug/receiver-app-debug.apk` |

### 19.5 OCR 说明

当前 APK 使用内置桩实现（返回图片尺寸信息作为占位）。接入真实 OCR 需：
- 方案A: tess-two (com.rmtheis:tess-two) + chi_sim_fast.traineddata (2.5MB) 放入 assets/tessdata/
- 方案B: Google ML Kit Text Recognition (com.google.mlkit:text-recognition-chinese)
- 替换 `TesseractOcrEngine.kt` 中的 `recognize()` 方法即可，其余代码不变

---

## 20. 生产部署记录（2026-05-27）

### 20.1 服务器信息

| 项目 | 值 |
|------|-----|
| 服务器 | 腾讯云 4核4G |
| 操作系统 | Windows Server 2022 Datacenter |
| 公网 IP | 139.199.11.252 |
| Python | 3.12.8 |
| HTTP 端口 | 9006 |
| WebSocket 端口 | 9007 |
| 项目路径 | C:\XU\ |
| 数据库 | SQLite (WAL模式) |

### 20.2 部署文件清单

```
C:\XU\
├── relay_server/      # 中转服务器代码
├── shared/            # 共享协议层
└── start_server.bat   # 一键启动脚本
```

### 20.3 启动命令

```powershell
pip install websockets Pillow   # 首次安装依赖
New-NetFirewallRule -DisplayName "闲鱼HTTP" -Direction Inbound -LocalPort 9006 -Protocol TCP -Action Allow
New-NetFirewallRule -DisplayName "闲鱼WS"   -Direction Inbound -LocalPort 9007 -Protocol TCP -Action Allow
```

双击 `start_server.bat` 或 `python relay_server/demo_server.py`

### 20.4 健康检查

```
GET http://139.199.11.252:9006/health
```

### 20.5 启动确认

```
HTTP 服务:    http://0.0.0.0:9006
WebSocket 服务: ws://0.0.0.0:9007
  SQLite 数据库: C:\XU\relay_server\data\events.db
```